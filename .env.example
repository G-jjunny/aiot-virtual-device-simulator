# livesim 실행에 필요한 환경변수. 복사해서 .env로 쓰세요 (.env는 커밋 금지).
#
# 컨테이너에서 호스트의 백엔드/브로커에 붙을 때는 localhost 대신
# host.docker.internal 을 쓰세요 (docker-compose.yml에 매핑되어 있습니다).

API_BASE_URL=
MQTT_HOST=
MQTT_PORT=

# 디바이스 시크릿 발급 권한이 있는 관리자 계정
ADMIN_USERNAME=
ADMIN_PASSWORD=
