.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
*.log

# 디바이스 시크릿 평문 — 절대 커밋 금지 (devices.example.yaml만 커밋)
devices.yaml

# 러너와 ctl이 주고받는 런타임 상태
control/

# 로컬 실행용 자격증명 — 절대 커밋 금지
.env
.env.*
!.env.example
